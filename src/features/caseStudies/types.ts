/**
 * Case Studies currently reuse `CaseStudyDetail` from `mocks/caseStudies.mock`
 * and the shared `SearchResultItem` shape. Add case-study-specific types
 * here once the Django `case_studies` endpoints are live (SRS section 8).
 */
export {};
